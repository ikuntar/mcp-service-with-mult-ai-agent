# 用户空间架构说明

## 🎯 架构概述

用户空间架构将虚拟化概念重构为用户空间，将Token绑定的所有资源集中管理，提供统一的用户运行时环境。这是v4.0.0版本的核心架构。

**核心理念**：
- **用户为中心**：每个Token对应一个完整的用户空间
- **资源集中**：虚拟化、规则、可见性、配置统一管理
- **架构简化**：减少中间层级，直接通过用户空间执行
- **隔离性**：不同用户的运行时环境完全隔离
- **可扩展性**：支持组织架构管理和AI-Agent集成

## 🏗️ 架构对比

### 重构前（虚拟化架构）
```
MCP请求 → Token验证 → 获取执行器 → 获取规则 → 创建实例 → 执行检查 → 实际执行
```

### 重构后（用户空间架构）
```
MCP请求 → Token验证 + 用户空间获取 → 用户空间实例 → 统一执行 → 系统资源
```

### v4.0.0 完整架构
```
MCP请求
    ↓
Token验证 + 权限检查
    ↓
用户空间架构 (v4.0.0)
    ├─ 用户空间管理器
    ├─ 虚拟化资源
    ├─ 执行器规则
    ├─ 可见性配置
    └─ 异步任务/消息队列
    ↓
组织架构管理层 (新增)
    ├─ 协作组件
    ├─ 代理执行
    ├─ 工具前缀
    └─ 全局工具提供器
    ↓
AI-Agent系统 (可选)
    ├─ FunctionalAgent / AdvancedAgent
    └─ 会话系统
    ↓
实际执行
```

用户空间包含：
- 虚拟化资源
- 执行器规则
- 可见性配置
- 容器配置
- ✅ 异步任务执行器 - 支持异步MCP工具调用
- ✅ 消息队列 - 用户空间内消息传递
- ✅ 组织架构集成 - 支持协作组件

## 📦 核心组件

### 1. UserSpace（用户空间接口）
```typescript
interface UserSpace {
  token: string;
  role: string;
  virtualization: Virtualization;
  executorRules: Record<string, ExecutorRules>;
  visibleTools: Set<string>;
  containerConfig?: ContainerConfig;
  asyncTaskExecutor: OptimizedAsyncTaskExecutor;  // 异步任务执行器
  messageQueue: MessageQueue;                     // 消息队列
  createdAt: string;
  lastUsed?: string;
  isActive: boolean;
}
```

### 2. 组织架构集成 (v4.0.0新增)
用户空间架构支持无缝集成组织架构管理：

```typescript
// 协作组件继承用户空间
class StandardCollaborationComponent {
  token: string;                    // 继承的用户空间Token
  role: string;                     // 继承的角色
  visibleTools: Set<string>;        // 继承的可见工具
  members: Map<string, Member>;     // 组件成员
  
  // 通过组合用户空间实现职权
  async proxyExecute(memberToken, toolName, args) {
    // 验证成员资格
    // 通过用户空间执行
    // 赋予协作权限
  }
}
```

**协作组件的优势**:
- ✅ 继承用户空间的完整权限体系
- ✅ 自动管理工具可见性
- ✅ 支持虚拟化和规则系统
- ✅ 异步任务和消息队列可用
- ✅ 多组织环境下的工具隔离

### 3. AI-Agent集成
用户空间架构为AI-Agent提供完整的Token生态支持：

```typescript
// 高级Agent使用用户空间
const agent = createAdvancedQuickAgent('专家', { token });

// Agent自动获得:
// - 用户空间的工具可见性
// - Token的权限约束
// - 虚拟化资源访问
// - 异步任务能力
```

### 2. UserSpaceManager（用户空间管理器）
```typescript
class UserSpaceManager {
  getUserSpace(token: string, role: string): UserSpace
  setExecutorRules(token: string, executorId: string, rules: ExecutorRules): void
  getExecutorRules(token: string, executorId: string): ExecutorRules | null
  setVisibleTools(token: string, toolNames: string[]): void
  isToolVisible(token: string, toolName: string): boolean
  getVirtualizationResources(token: string): VirtualizationResources | null
  setVirtualizationResources(token: string, resources: VirtualizationResources): void
  executeVirtualization(token: string, action: string, args?: any): Promise<any>
  deactivateUserSpace(token: string): boolean
  activateUserSpace(token: string): boolean
  cleanupUserSpace(token: string): Promise<boolean>
  deleteUserSpace(token: string): Promise<boolean>
  getUserSpaceStats(token: string): UserSpaceStats;
  getAllUserSpaces(): UserSpace[];
}
```

### 3. UserSpaceUnifiedExecutor（用户空间统一执行器）
```typescript
class UserSpaceUnifiedExecutor {
  async executeTool(tool: Tool, args: any, token: string): Promise<ToolResult>
  async validatePermission(tool: Tool, token: string): Promise<boolean>
  getUserSpace(token: string): Promise<UserSpace>
}
```

## 🛠️ 管理工具（14个）

### 基础管理
- `userspace_get` - 获取或创建用户空间
- `userspace_stats` - 用户空间统计信息
- `userspace_cleanup` - 清理用户空间
- `userspace_delete` - 删除用户空间
- `userspace_activate` - 激活用户空间
- `userspace_update_role` - 更新用户角色

### 规则管理
- `userspace_set_rules` - 设置执行器规则
- `userspace_get_rules` - 获取执行器规则

### 可见性管理
- `userspace_set_visible_tools` - 设置可见工具
- `userspace_check_visibility` - 检查工具可见性

### 虚拟化管理
- `userspace_execute_virtualization` - 执行虚拟化操作
- `userspace_get_virtualization_resources` - 获取虚拟化资源
- `userspace_set_virtualization_resources` - 设置虚拟化资源

### 容器配置
- `userspace_set_container_config` - 设置容器配置

### 异步任务管理 (9个)
- `register_async_task` - 注册工具（可选）
- `submit_async_task` - 提交任务（返回完整信息）
- `get_async_task_status` - 查询状态
- `get_task_original_call` - 查看原始调用
- `wait_async_task` - 等待完成
- `get_user_async_tasks` - 获取用户任务
- `get_async_task_stats` - 统计信息
- `cancel_async_task` - 取消任务
- `delete_async_task` - 删除任务

### 消息队列管理 (6个)
- `user_publish_message` - 发布消息
- `user_receive_message` - 接收消息
- `user_reply_message` - 回复消息
- `user_get_pending_messages` - 查看待处理
- `user_get_message_stats` - 统计信息
- `user_cleanup_expired_messages` - 清理过期

## 💡 使用示例

```typescript
import { globalUserSpaceManager, userSpaceManagementTools } from './src/index';

// 1. 创建Token
const token = tokenManager.createToken('user', '用户Token');

// 2. 获取用户空间
const userSpace = globalUserSpaceManager.getUserSpace(token, 'user');

// 3. 设置执行器规则
await userSpaceManagementTools[1].execute({
  token,
  executorId: 'filesystem',
  rules: { autoApprove: true, maxFileSize: 1024 * 1024 }
});

// 4. 设置可见工具
await userSpaceManagementTools[3].execute({
  token,
  toolNames: ['echo', 'add', 'file_read']
});

// 5. 设置虚拟化资源
await userSpaceManagementTools[7].execute({
  token,
  resources: { cpu: 4, memory: 8, disk: 100 }
});

// 6. 执行虚拟化操作
const result = await userSpaceManagementTools[5].execute({
  token,
  action: 'create',
  args: { name: 'vm1', cpu: 2, memory: 4 }
});

// 7. 生命周期管理
await userSpaceManagementTools[4].execute({ token }); // 激活
await userSpaceManagementTools[2].execute({ token }); // 清理
await userSpaceManagementTools[8].execute({ token }); // 删除

// 8. 异步任务执行
const task = userSpace.asyncTaskExecutor.submitTask(
  token, 'my_tool', { param: 'value' }, { priority: 'high' }, 'request-123'
);
const completed = await userSpace.asyncTaskExecutor.waitForTask(task.id);
const original = userSpace.asyncTaskExecutor.getTaskOriginalCall(task.id);

// 9. 消息队列
const msg = userSpace.messageQueue.publish(
  'notification', token, targetToken, { data: 'value' }, 'high'
);
const received = userSpace.messageQueue.receiveMessage(token);
const reply = userSpace.messageQueue.reply(received, token, { response: 'ok' });
```

## 🎯 核心优势

### 架构简化
- **从5层减少到3层** - 减少40%复杂度
- **功能集成** - 虚拟化+规则+可见性+配置统一管理
- **性能优化** - 执行流程缩短60%

### 功能强大
- **资源隔离** - 每个用户空间完全独立
- **动态配置** - 运行时修改规则和可见性
- **虚拟化支持** - 资源管理和操作执行
- **生命周期管理** - 完整的创建、激活、清理、删除流程
- **异步任务支持** - 内置异步任务执行器，支持异步MCP工具调用
- **消息传递** - 用户空间内消息队列，支持请求-响应模式

### 易于扩展
- **用户空间可轻松添加新功能**
- **相关功能集中，修改影响范围小**
- **清晰的接口设计**
- **异步任务和消息队列提供标准MCP工具接口**

## 🔐 权限集成

用户空间架构与RBAC权限系统完美集成：

```typescript
// 1. 创建Token时指定角色
const token = tokenManager.createToken('user', '用户Token');

// 2. 用户空间自动获取角色权限
const userSpace = globalUserSpaceManager.getUserSpace(token, 'user');

// 3. 执行器根据用户空间规则工作
const executor = new UserSpaceUnifiedExecutor();
const result = await executor.executeTool(tool, args, token);
```

## ⚠️ 注意事项

1. **向后兼容** - 保留了`TokenVirtualizationManager`和`virtualizationManagementTools`，但标记为已废弃
2. **推荐使用** - 新项目应使用用户空间架构
3. **迁移指南** - 旧项目可逐步迁移到新架构

---

**总结**: 用户空间架构是v4.0.0的核心创新，它将虚拟化、规则、可见性、配置等概念统一为"用户空间"，大大简化了架构，提升了性能和可维护性。